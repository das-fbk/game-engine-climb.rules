# insert your constants to use in game rules
# 
# SAMPLE USAGE 
# 
# constants file
# var_name = var_value
# 
#
# usage in rule
# global VALUE_TYPE var_name;
#
# supported VALUE_TYPE
# Integer (i.e. var=1)
# Double  (i.e. var=2.3)
# String  (i.e. var=park)
# java.util.ArrayList (i.e var=park,street,bikestation)

# constants for modal distance (except for pedibus)
# const_pandr_distance = 100.0
# const_bus_distance = 1800.0
# const_zeroimpact_distance = 500.0

# constants for meteo bonuses
# const_cloudy_bonus = 50.0
# const_rain_bonus = 100.0
# const_snow_bonus = 200.0

#for zero impact trips taken autonomously
const_zi_solo_bonus = 100.0  

#bonus for reaching leg (school)
const_leg_bonus=5000.0

#bonus for beating weekly record (as percent)
const_record_percent=0.05

#multiplier for pedibus trips
const_pedibus_multiplier = 1.5 

#name of the school team/player
const_school_name=scuola


# "almost" distance from next leg (in meters, used in notifications)
const_almost_reached_next_leg=900.0

# nominal weekly distance estimated for whole school
const_weekly_nominal_distance=10000.0

#constants representing distances to legs 
leg_1_distance=1000.0
leg_2_distance=10000.0
leg_3_distance=20000.0
leg_4_distance=30000.0
leg_5_distance=40000.0

